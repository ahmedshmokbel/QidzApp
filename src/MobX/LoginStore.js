export function createLoginStore() {

    return {
        isLoggedIn: false,
        login() {
            store.isLoggedIn = true;
        },
        logout() {
            store.bugs.length;
        }
    }
}